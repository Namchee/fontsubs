/**
 * Subset fonts from the given path
 *
 * @param {string} path - Filepath to scan for files
 * @param {string?} whitelist - List of unicode range to target.
 * Providing this parameter will skip unicode scanning.
 */
export function subsetFonts(path: string, whitelist?: string) {

}
